let min = 60;

   if (min >= 0 && min <= 20) {
      console.log('1 четверть');
   }

   if (min >= 21 && min <= 41) {
      console.log('2 четверть');
   }

   if (min >= 42 && min <= 60) {
      console.log('3 четверть');
   }